computeResults(10)
function computeResults(x) {
    let y = formatResults(x)
    console.log(gl1 + " " + y)
    function formatResults(inp) {
        return String(inp)
    }
}

var gl1 = "GlobaContext1"